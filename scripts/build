#!/usr/bin/env bash
set -euo pipefail

root_dir=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)
cd "$root_dir"

INVERO_ROOT="$root_dir" nvim --headless --clean \
  --cmd 'lua vim.opt.runtimepath:prepend(vim.env.INVERO_ROOT)' \
  "+lua require('invero.extras').setup()" \
  +qa
